# Late Web UI

React + Vite + Tailwind v4 SPA for late.kodingvibes.com (Icecast player + IRC client).

## Dev

```bash
npm install
npm run dev
```

## Build

```bash
npm run build        # outputs dist/ (static SPA, served by nginx)
```

## Lint

```bash
npm run lint         # tsc --noEmit
