/**
 * Bump this when shipping a user-visible change (feature or fix).
 * Rendered in the header so it's easy to confirm a deploy took
 * effect by hard-reloading the page.
 */
export const APP_VERSION = 'v1.1.0'
